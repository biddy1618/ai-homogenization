# Core pipeline dependencies for the Cross Validated homogenization prototype
lxml>=5.0
beautifulsoup4>=4.12
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8
pyarrow>=15.0
tqdm>=4.66
# Optional: true contextual embeddings (Sentence-BERT/MiniLM via ONNX, no PyTorch)
fastembed>=0.8
# Optional: dynamic semantic topic modeling (install bertopic with --no-deps to avoid torch)
bertopic>=0.17
umap-learn>=0.5
hdbscan>=0.8
# Optional: extract Stack Exchange .7z dumps
py7zr>=0.21
